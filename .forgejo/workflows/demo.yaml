on:
  workflow_dispatch:
  push:
    branches:
      - main-next

jobs:
  test:
    runs-on: docker
    steps:
      - run: echo All good!
      - run: uname -a
      - run: cat /etc/os-release
      - run: df -h
      - run: free -h
      - run: dpkg -l